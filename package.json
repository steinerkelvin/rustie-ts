{
  "name": "rustie",
  "version": "0.1.0",
  "description": "Helper library to type and safely handle Rust's `serde` JSON serialization of Enums. Can also be used standalone to implement Enums in TypeScript with plain objects.",
  "repository": "https://github.com/steinerkelvin/rustie-js",
  "author": "Kelvin Steiner <me@steinerkelvin.dev>",
  "license": "MIT",
  "private": false,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "prepublish": "tsc",
    "build": "tsc",
    "format": "prettier --write ./src",
    "format-check": "prettier --check ./src",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "prettier": "^2.7.1",
    "typescript": "^4.7.4"
  }
}
